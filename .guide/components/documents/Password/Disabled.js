import React from 'react';
import { Password } from '@fork-ui/components/core';

const Demo = () => {
  return (
    <Password
      disabled
      placeholder="The placeholder"
    />
  );
};

export default Demo;
