import React from 'react';
import { Password } from '@/fork-ui/core';

const Demo = () => {
  return (
    <Password
      disabled
      placeholder="The placeholder"
    />
  );
};

export default Demo;
