import React from 'react';
import { Wrapper } from '@fork-guide/components';
import { Pincode } from '@fork-ui/core';

export default () => {
  return (
    <Wrapper>
      <Pincode length={4} />
    </Wrapper>
  );
};
