import React from 'react';
import { Wrapper } from '@fork-guide/components';
import { Select } from '@fork-ui/core';

export default () => {
  return (
    <Wrapper>
      <Select />
    </Wrapper>
  );
};
