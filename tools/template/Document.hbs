import React from 'react';
import { Anchor } from '@/rc-neumorphism/core';
import Codebox from '@/components/Codebox';

{{#each importers}}
import {{this}} from './demo/{{this}}';
{{/each}}

export default () => {
  return (
    <div className="flex">
      <div className="flex flex-col flex-1">
        {{#each importers}}
         <Codebox className="mb-1" Component={ {{this}} } />
        {{/each}}
      </div>
      <div className="pl-1" style={ { flexBasis: '200px' } }>
        <Anchor
          top={75}
          style={ {
            backgroundColor: 'var(--rc-color--300)',
            height: 'calc(100vh - 75px',
            borderRadius: '0.5rem',
          } }
        >
          {{#each importers}}
           <Anchor.Link key={`#${ {{this}}.href}` } title={ {{this}}.header } />
          {{/each}}
        </Anchor>
      </div>
    </div>
  );
};
