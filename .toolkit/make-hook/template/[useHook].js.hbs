import { useState, useCallback, useMemo, useEffect } from 'react';

const {{useHook}} = ({}) => {
  const [value, setState] = useState('');
  const onFuncName = useCallback(() => {

  }, []);
  const memo = useMemo(() => ({

  }), []);

  useEffect(() => {
    // TODO
    return () => {
      // TODO
    };
  }, []);

  return {

  };
};

export default {{useHook}}
