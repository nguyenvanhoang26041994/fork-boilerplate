export const Colors = {
  code: `import React from 'react';
import { Wrapper } from '@style-guide/components';
import { Colors } from '@fork-ui/core';

export default () => {
  return (
    <Wrapper>
      <Colors />
    </Wrapper>
  );
};
`,
  demoName: 'Colors',
}