export const Dialog = {
  code: `import React from 'react';
import { Wrapper } from '@style-guide/components';
import { Dialog } from '@fork-ui/core';

export default () => {
  return (
    <Wrapper>
      <Dialog />
    </Wrapper>
  );
};
`,
  demoName: 'Dialog',
}