export const Tabs = {
  code: `import React from 'react';
import { Wrapper } from '@style-guide/components';
import { Tabs } from '@fork-ui/core';

export default () => {
  return (
    <Wrapper>
      <Tabs />
    </Wrapper>
  );
};
`,
  demoName: 'Tabs',
}